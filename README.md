# Operari Landing Page

Modern single-page website for Operari — AI & SaaS Technology Company.

## Tech

- HTML5
- CSS (custom properties, 3D transforms, glassmorphism)
- Vanilla JavaScript (client-side routing, tilt cards, search overlay)

## Develop locally

```bash
# Option A — simple http server
npx serve public

# Option B — with live reload (recommended)
npm install -g live-server
live-server public
